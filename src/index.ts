import './card';
import './editor';

window.customCards = window.customCards ?? [];
window.customCards.push({
  type: 'klyqa-pet-card',
  name: 'Klyqa Pet Card',
  description: 'Displays and controls a single Klyqa Pet device (Welly, Foody, Air Klyna).',
  preview: true,
});
